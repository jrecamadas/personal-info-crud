const data = state => state.data;
const formatted = state => state.formatted;
const defaultVal = state => state.defaultVal;
const ready = state => state.ready;

export {
    data,
    formatted,
    defaultVal,
    ready
}
