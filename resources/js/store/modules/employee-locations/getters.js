const data = state => state.data;
const formatted = state => state.formatted;
const location = state => state.location;
const formatted_location = state => state.formatted_location;

export {
    data,
    formatted,
    formatted_location,
    location
}