const data = state => state.data;
const formatted = state => state.formatted;
const meta = state => state.meta;
const reportTemplate = state => state.reportTemplate
const reportTemplateFilter = state => state.reportTemplateFilter

export {
    data,
    formatted,
    meta,
    reportTemplate,
    reportTemplateFilter
}
