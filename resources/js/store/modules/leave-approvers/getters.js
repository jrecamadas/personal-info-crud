const data = state => state.data;
const formatted = state => state.formatted;
const meta = state => state.meta;
const approver = state => state.approver;
const approversAll = state => state.approversAll;

export {
    data,
    formatted,
    meta,
    approver,
    approversAll
}
