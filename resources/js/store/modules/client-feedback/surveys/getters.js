const data = state => state.data;
const formatted = state => state.formatted;
const surveyMeta = state => state.surveyMeta;
const survey = state => state.survey;
const surveyData = state => state.surveyData;

export {
    data, formatted, surveyMeta, survey, surveyData,
};
