const data = state => state.data;
const formatted = state => state.formatted;
const meta = state => state.meta;
const categories = state => state.categories;

export {
    data, formatted, meta, categories,
};
