const data = state => state.data;
const formatted = state => state.formatted;
const employee_work_shift = state => state.employee_work_shift;

export {
    data,
    formatted,
    employee_work_shift
}