const parseHistory = state => state.parseHistory
const formatted = state => state.formatted
const dropdownIndeces = state => state.dropdownIndeces
const meta =  state => state.meta

export {
    parseHistory,
    formatted,
    dropdownIndeces,
    meta,
}
